# NORSEMAN YACHTS 官网 - 快速启动指南

## 🎯 项目概述

这是一个完整的高端游艇品牌官网项目，展示了北欧探险精神与极致工艺。

## 📋 前置要求

在开始之前，请确保您的系统已安装：

- **Node.js** (版本 14 或更高)
  - 下载地址: https://nodejs.org/
  - 验证安装: `node --version`

- **npm** (通常随 Node.js 一起安装)
  - 验证安装: `npm --version`

## 🚀 启动步骤

### 步骤 1: 安装依赖

在项目根目录下打开终端，运行：

```bash
npm install
```

此命令将安装所有必需的依赖包，包括：
- React 及相关库
- Tailwind CSS
- Framer Motion
- React Router
- 以及其他工具库

安装过程可能需要 2-5 分钟，取决于您的网络速度。

### 步骤 2: 启动开发服务器

安装完成后，运行：

```bash
npm start
```

此命令会：
1. 启动开发服务器
2. 自动打开浏览器访问 `http://localhost:3000`
3. 启用热重载（修改代码后自动刷新）

### 步骤 3: 浏览网站

浏览器会自动打开，您可以看到：

- **首页** - 展示品牌核心理念和视觉冲击
- **Yachts** - 三大系列游艇展示
- **Story** - 品牌历史与设计哲学
- **Events** - 全球活动日历
- **Network** - 全球合作伙伴网络
- **Contact** - 联系表单

## 📱 测试响应式设计

### 浏览器开发者工具
1. 按 F12 打开开发者工具
2. 点击设备工具栏图标（或按 Ctrl+Shift+M）
3. 选择不同设备尺寸测试

### 推荐测试的设备尺寸
- **桌面**: 1920x1080, 1440x900
- **平板**: iPad (768x1024), iPad Pro (1024x1366)
- **手机**: iPhone 12 (390x844), Samsung Galaxy S20 (360x800)

## 🎨 主要功能测试

### 导航栏
- ✅ 滚动时背景变化效果
- ✅ 活动路由高亮显示
- ✅ 移动端菜单展开/收起

### 动画效果
- ✅ 首页滚动指示器动画
- ✅ 页面切换平滑过渡
- ✅ 内容滚动触发淡入动画
- ✅ 卡片悬停缩放效果

### 表单功能（Contact 页面）
- ✅ 实时表单验证
- ✅ 必填字段检查
- ✅ 邮箱格式验证
- ✅ 最小字符数验证
- ✅ 错误提示显示

## 🛠️ 常见问题

### 问题 1: 端口 3000 已被占用

**错误信息**: "Something is already running on port 3000"

**解决方案**:
- 选项 A: 按 Y 使用其他端口
- 选项 B: 关闭占用 3000 端口的程序

### 问题 2: 依赖安装失败

**解决方案**:
```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 问题 3: 样式不显示

**解决方案**:
- 确保 Tailwind CSS 正确安装
- 重启开发服务器
- 清除浏览器缓存 (Ctrl+Shift+R)

## 📦 构建生产版本

当您准备部署时：

```bash
# 创建优化的生产构建
npm run build

# 构建完成后，build/ 文件夹包含可部署的文件
```

## 🎯 下一步

### 定制内容
1. **修改船型数据** - 编辑 `src/pages/Yachts.jsx`
2. **更新活动信息** - 编辑 `src/pages/Events.jsx`
3. **修改联系方式** - 编辑 `src/pages/Contact.jsx`

### 添加真实图片
当前使用 Unsplash 占位图片，您可以：
1. 在 `public/images/` 创建文件夹
2. 放置您的图片
3. 更新各组件中的图片路径

### 集成后端 API
在 `src/pages/Contact.jsx` 的 `onSubmit` 函数中：
```javascript
const onSubmit = async (data) => {
  try {
    const response = await fetch('YOUR_API_ENDPOINT', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data)
    });
    // 处理响应
  } catch (error) {
    // 错误处理
  }
};
```

## 📚 学习资源

- **React 官方文档**: https://react.dev/
- **Tailwind CSS**: https://tailwindcss.com/docs
- **Framer Motion**: https://www.framer.com/motion/
- **React Router**: https://reactrouter.com/

## 💡 技巧提示

### 开发体验优化
- 使用 VS Code 的 Tailwind CSS IntelliSense 插件
- 安装 ES7+ React/Redux/React-Native snippets
- 启用 Prettier 代码格式化

### 性能优化建议
- 图片使用 WebP 格式
- 实施代码分割 (React.lazy)
- 使用 React.memo 优化组件渲染
- 添加图片懒加载

## 🎉 开始探索

现在您已经准备好了！打开浏览器，尽情探索这个充满北欧探险精神的网站吧。

**祝您开发愉快！** ⛵

---

如有任何问题，请查看完整的 README.md 或提交 Issue。
